# Frontend Environment Variables
# This file is OPTIONAL - the frontend defaults to http://localhost:8080

# Backend API URL
# Default: http://localhost:8080 (used if not set)
# Only set this if your backend runs on a different URL
VITE_API_URL=http://localhost:8080
